{
  "$id": "https://github.com/data-fair/catalog-grist/catalog-config",
  "x-exports": [
    "types",
    "validate",
    "schema"
  ],
  "title": "GristConfig",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "url",
    "apiKey"
  ],
  "properties": {
    "url": {
      "type": "string",
      "title": "URL",
      "description": "The url of the catalog. The url should not**pas** contains the `/o/name-of-team-space`",
      "x-i18n-description": {
        "fr": "L'URL de l'API du catalogue. L'URL ne doit **pas** contenir de `/o/nom-de-l-espace-d-equipe`"
      },
      "pattern": "^https?://.*[^/]$",
      "errorMessage": "The URL must start with http:// or https:// and must not end with `/`.",
      "x-i18n-errorMessage": {
        "fr": "L'URL doit commencer par http:// ou https:// et ne pas se terminer par `/`."
      },
      "examples": [
        "https://grist.numerique.gouv.fr"
      ]
    },
    "apiKey": {
      "type": "string",
      "title": "clé d'API",
      "description": "Your api KEY, accessible from your **Profile Settings**. Create, copy and paste here your API key.",
      "x-i18n-description": {
        "fr": "Votre clé d'API, accessible depuis vos **Paramètres du compte**. Créer, copier et coller ici votre clé d'API."
      },
      "layout": {
        "props": {
          "type": "password",
          "autocomplete": "new-password"
        }
      }
    }
  }
}